# Frontend

React + TypeScript + Vite frontend for the AI Workflow Evaluator.

## Setup

```bash
npm install
npm run dev    # http://localhost:5173
npm test       # run tests once
npm run build  # production build
```

Requires the backend running at `http://localhost:8000`.
